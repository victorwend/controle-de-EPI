# Sistema de Controle de EPIs

Projeto acadêmico e prático para desenvolver um sistema de controle de Equipamentos de Proteção Individual (EPIs).

O front-end começou a ser construído em 06/09/2026 (tela de Login). O restante do sistema ainda não tem código.

## Stack

- **Front-end:** React + Vite + Tailwind CSS (mesma convenção dos demais projetos do hub). Ver `frontend/`.
- **Back-end:** Supabase (Postgres + Auth + RLS), mesma convenção dos demais projetos do hub. Schema em `supabase/` (numerado, um arquivo por fatia — ver changelog de 17/09/2026 pra decisão de arquitetura multi-tenant).

## Objetivo

Organizar o desenvolvimento de um sistema capaz de apoiar, futuramente:

- cadastro de funcionários, obras e EPIs;
- controle de entrada, saída e saldo de estoque;
- registro de entrega, devolução e troca de EPIs;
- controle de CA e validade;
- transferência de funcionários e materiais entre obras;
- geração de históricos e relatórios;
- confirmação de recebimento por assinatura ou biometria.

## Organização do repositório

| Pasta | Finalidade |
|---|---|
| `docs/01-visao-geral` | Objetivos, escopo e visão do produto |
| `docs/02-requisitos` | Requisitos funcionais, não funcionais e regras de negócio |
| `docs/03-processos` | Fluxos de trabalho e processos da empresa |
| `docs/04-ux-ui` | Protótipos, telas, pesquisas e decisões de usabilidade |
| `docs/05-arquitetura` | Decisões técnicas e diagramas da solução |
| `docs/06-banco-de-dados` | Modelos conceitual, lógico e físico |
| `docs/07-testes` | Planos, cenários e resultados de testes |
| `docs/08-seguranca` | Permissões, riscos e cuidados com os dados |
| `docs/09-reunioes` | Atas, decisões e acompanhamentos |
| `docs/10-aprendizado` | Anotações da faculdade aplicadas ao projeto |
| `gestao/backlog` | Ideias e tarefas que ainda serão priorizadas |
| `gestao/sprints` | Planejamento e acompanhamento dos ciclos de trabalho |
| `gestao/modelos` | Modelos reutilizáveis de tarefa, reunião e documentação |
| `assets` | Imagens, diagramas e materiais visuais |
| `backend` | Espaço reservado para o código do servidor |
| `frontend` | Interface do sistema (React + Vite + Tailwind) |
| `tests` | Espaço reservado para os testes automatizados |

## Situação atual

- [x] Repositório criado
- [x] Estrutura de pastas organizada
- [ ] Documentar a visão do produto
- [ ] Levantar requisitos
- [ ] Definir regras de negócio
- [ ] Modelar o banco de dados
- [ ] Definir a arquitetura
- [x] Iniciar o desenvolvimento (front-end, tela de Login)

## Changelog

- **17/09/2026 — EPIs e CAs migrado (Lista e Novo EPI; quarto módulo real).** `supabase/05_epis.sql`: tabela `epis` (CA único por empresa). **Diferença em relação às RLS anteriores:** o catálogo de perfis (10.03) diz explicitamente que "Técnico de Segurança" cuida de "EPIs, CAs e relatórios" — então a policy de gerenciamento inclui esse perfil além de `administrador`, não é admin-only como `obras`/`funcionarios` foram. **Categoria** virou texto livre no formulário (Categoria de EPI continua sendo cadastro mock, sem tabela própria — mesmo gap de sempre). O **Status** (Válido/A vencer/Vencido) não é uma coluna: é calculado no front a partir de `validade_ca`, com 60 dias de antecedência como default provisório — RN079 já deixava esse prazo como "configurável" e sem valor definido, então isso vira campo de configuração de verdade só quando a aba 10.06 (Alertas) for implementada. `EpisLista.jsx`/`NovoEpi.jsx` não usam mais `episConfig.js`; o arquivo continua existindo porque `LoteEntrega`, `NovaEntrega`, `EntradaEstoque`, `TransferenciaEstoque` e `MatrizEpiFuncao` (fora de escopo desta fatia) ainda dependem dele. Testado ao vivo contra o Supabase real: EPI com validade próxima e outro distante, status mudando de cor certo na lista.
- **17/09/2026 — Funcionários migrado (Lista, Novo e Ficha; terceiro módulo real).** `supabase/04_funcionarios.sql`: tabela `funcionarios` (matrícula única por empresa, `obra_id` como FK real pra `obras`, RLS igual ao padrão de `obras`/`usuarios` — só `administrador` gerencia). Campos **Função** e **Setor** viraram texto livre no formulário (`NovoFuncionario.jsx`) em vez dos selects vazios do protótipo — Cargo e Setor continuam sendo cadastros mock, sem tabela própria, mesmo gap já documentado pra Obra/Local de estoque. O select de **Obra** agora é real, carregado do Supabase. `FuncionariosLista.jsx`, `NovoFuncionario.jsx`, `FichaFuncionario.jsx` e `HistoricoCompleto.jsx` não usam mais `funcionariosConfig.js`/`FUNCIONARIOS`. **"EPIs ativos", "Pendências" e "Última entrega" mostram 0/— honestamente em todos os lugares** (Lista, Ficha, Histórico) — dependem do módulo de Entregas, ainda 100% mock. "Transferir funcionário" (04.05) continua desabilitado, sem mudança. **Fora de escopo desta fatia, ainda mock de propósito:** `LoteEntrega.jsx`, `TrocaDevolucao.jsx`, `NovaEntrega.jsx`, `BiometriaCadastro.jsx` (módulo Entregas) e `FichaEpiIndividual.jsx` (Relatórios) continuam lendo `FUNCIONARIOS` do arquivo mock — por isso ele não foi apagado, só parou de ser usado pelas telas de Funcionários. Testado ao vivo contra o Supabase real: funcionário criado e vinculado à obra, aparecendo certo na Lista e na Ficha.
- **17/09/2026 — Dashboard migrado (primeiro módulo a sair do mock).** `Dashboard.jsx` parou de importar `DASHBOARD_KPIS`/`DASHBOARD_ALERTAS` (arquivo `dashboardConfig.js` apagado, ficou sem nenhum uso) e `ENTREGAS` mockado. Como as tabelas de entregas/estoque/EPIs/CAs ainda não existem no banco, os 4 KPIs (EPIs em estoque, Entregas no mês, Estoque crítico, CAs a vencer) agora mostram `0` com uma nota honesta ("ainda não implementado"/"nenhuma entrega registrada") em vez do número de mentira do protótipo — mesmo princípio de "estado vazio honesto" que o projeto já usa em outras telas. "Movimentações recentes" e "Alertas e pendências" viraram estados vazios de verdade, com CTA pra registrar a primeira entrega. O único dado 100% real do Dashboard hoje é a contagem de obras (`X obra(s) cadastrada(s)`, consulta direta ao Supabase `obras`, escopada por RLS) — colocada como texto acima dos KPIs, não como um 5º KPI, pra não misturar com os 4 indicadores que o Figma define e que ainda não têm dado real por trás. Testado ao vivo contra o Supabase real, confirmado pelo usuário.
- **17/09/2026 — Obra real + trava de onboarding (empresa nova não vê mais dado de mentira).** Depois do primeiro teste de cadastro ao vivo, ficou claro que uma empresa recém-criada caía direto na Visão geral com os dados mockados de sempre (João Carlos e cia.) — porque só a autenticação tinha virado real; todo o resto (Dashboard, Funcionários, Estoque...) continua sendo o protótipo com arrays fixos em `frontend/src/data/*.js`, sem saber que existe uma empresa nova vazia. Resolvido só para Obra por enquanto (é o bloqueio de fato — sem obra, nada mais faz sentido): `supabase/03_obras.sql` estende a tabela `obras` mínima com os campos reais do cadastro 09.02 (código, centro de custo, cidade/UF, responsável — este último como texto livre, mesmo gap de "sem Funcionários ainda" que os outros cadastros já tinham) e corrige o domínio de status para `ativa`/`inativa` (o que o formulário realmente usa). Novo `frontend/src/context/AuthContext.jsx` (sessão + perfil de `usuarios` + `temObra`, via `onAuthStateChange`) e `frontend/src/components/RequireAuth.jsx` (rota-layout do React Router): sem sessão → volta pro Login; com sessão mas `temObra === false` e fora de `/obras/nova` → redireciona pra lá, travando o resto do app até existir pelo menos uma obra. `frontend/src/pages/obras/NovaObra.jsx` é a primeira tela do app que já lê/grava direto no Supabase (`obras`, escopada por `empresa_id` do usuário logado) — substituiu o antigo cadastro mock de Obra (`cadastrosConfig.js` perdeu a entrada `obra`, os links de "Obras" no hub de Cadastros e na lista de Obras agora apontam pra `/obras/nova`). **Ainda mock, de propósito, fora de escopo desta fatia:** `ObrasLista.jsx`/`ObraDetalhe.jsx` continuam lendo `obrasConfig.js` fixo, não a obra real recém-criada — e todo o resto do app (Funcionários, Estoque, EPIs, Entregas, Dashboard) segue 100% mock. Migrar cada módulo pra dado real por empresa é trabalho futuro, um de cada vez. **Testado ao vivo contra o Supabase real:** cadastro da empresa → login → trava redirecionando pra "Cadastre sua primeira obra" → obra salva com sucesso (depois de um erro de cache de schema do PostgREST no meio do caminho, resolvido com `NOTIFY pgrst, 'reload schema'`, sem relação com o código em si).
- **17/09/2026 — Tela de Cadastro da empresa + Login ligado ao Supabase Auth.** `frontend/src/pages/Cadastro.jsx` (rota `/cadastro`, mesma pasta e estilo visual do Login): pede Razão social + CNPJ (empresa), Nome completo + E-mail + Senha (Administrador) e uma seleção de **plano** (Starter/Profissional/Empresarial — sem checkout nesta fase, só grava a escolha via nova coluna `empresas.plano`, `supabase/02_empresas_plano.sql`, que recriou `criar_empresa_com_administrador()` com o parâmetro novo). Fluxo real: `supabase.auth.signUp()` → RPC `criar_empresa_com_administrador()` → `signOut()` → volta pro Login (`/`) com um aviso de sucesso, em vez de entrar direto — pedido explícito do usuário, mesmo sendo tecnicamente possível pular essa volta. `Login.jsx` deixou de ser só front (TODO da Sprint 3): agora chama `supabase.auth.signInWithPassword()` de verdade, mostra erro de credencial inválida e ganhou o link "Criar conta" para quem ainda não tem empresa. **Pendência consciente registrada no próprio formulário:** se o projeto Supabase tiver confirmação de e-mail ativada (padrão de projeto novo), o `signUp()` não retorna sessão ativa na hora e a criação da empresa não pode ser concluída nesse mesmo request — precisa desativar em Authentication → Providers enquanto for só ambiente de desenvolvimento, ou implementar a criação da empresa depois da confirmação (não feito ainda). Build (`npm run build`) verificado sem erros; teste ponta a ponta contra o Supabase real ainda pendente (falta rodar `01_` e `02_` no projeto).
- **17/09/2026 — Decisão de multi-tenant + schema inicial de empresas/usuários.** O produto vai ser vendido para várias empresas (não só a Pavidez, cliente único do protótipo Figma original) — decisão que muda a arquitetura de autenticação: o sistema passa a ser multi-tenant, com cada empresa se autocadastrando (em vez de depender de alguém criando conta manualmente para cada cliente). Mapeado o fluxo completo em `docs/05-arquitetura/plano-de-producao-telas.md`: uma tela nova de "Cadastro da empresa" (não existe no Figma, cria a empresa + já grava quem preencheu como Administrador) alimenta o Login (01.01, ainda sem backend) → Dashboard; dentro da empresa, o Administrador convida colaboradores via `09.06 — Cadastro de usuário` (perfil default "Operador", catálogo de 5 perfis extraído de `10.03 — Usuários e permissões`: Administrador/Almoxarife/Técnico de Segurança/Gestor/Operador) — convite por e-mail via Edge Function (ainda não implementada), não criação direta de senha pelo admin, para não expor a chave `service_role` no navegador. Primeira fatia do schema criada em `supabase/01_empresas_e_usuarios.sql`: tabelas `empresas`, `obras` (versão mínima), `usuarios` (1:1 com `auth.users`) e `usuario_obras`, com RLS escopando tudo por `empresa_id` e só `administrador` podendo gerenciar usuários/obras da própria empresa; RPC `criar_empresa_com_administrador()` faz o bootstrap logo após o `signUp()`. Detalhe do modelo em `docs/06-banco-de-dados/modelo-empresas-usuarios.md`. Front-end preparado com `@supabase/supabase-js`, `frontend/src/lib/supabaseClient.js` e `frontend/.env.example` — ainda falta rodar o SQL no projeto Supabase real e ligar o Login/Cadastro de fato. **Pendências conscientes:** sem tela de recuperação de senha (gap já registrado, risco 2 do plano de telas); `usuarios.status` (ativo/inativo) existe na tabela mas nenhuma policy ainda bloqueia usuário inativo.
- **07/09/2026 — Sprint 10 completa: Histórico, dashboard e relatórios (02.01, 04.03, 04.04, 08.01, 08.02).** Cinco telas novas. `Dashboard.jsx` (02.01, rota `/visao-geral`, antes um placeholder): 4 KPIs, "Movimentações recentes" reaproveitando o mesmo array `ENTREGAS` já usado pela lista de Entregas (os números batem exatamente), painel de alertas e 4 ações rápidas — tudo com os valores literais do Figma. `FichaFuncionario.jsx` (04.03, substitui o antigo placeholder `FuncionarioEmConstrucao.jsx` — apagado, sem mais uso) e `HistoricoCompleto.jsx` (04.04, novo, com filtro Todos/Entregas/Trocas/Devoluções): os dois só têm dado literal completo para João Carlos (00487) no Figma — os 4 funcionários restantes mostram um estado vazio honesto ("Nenhuma entrega registrada... neste mock") em vez de inventar histórico. `RelatoriosPainel.jsx` (08.01, rota `/relatorios`, antes um placeholder): KPIs, consumo por EPI e entregas por obra com os números exatos do protótipo, mais um botão "Exportar relatório" que gera um CSV real via `Blob`/`URL.createObjectURL` — sem biblioteca nova. `FichaEpiIndividual.jsx` (08.02, rota `/funcionarios/:matricula/ficha-epi`): documento de impressão/PDF com layout próprio (sem o menu lateral do `AppShell`, que não faz sentido numa folha impressa) e `window.print()` no botão "Exportar PDF / Imprimir". **Bug de copiar-colar corrigido:** no Figma, as seções "EPIs entregues ao colaborador" e "Termo de responsabilidade" desse frame reaproveitavam literalmente os números agregados de *todos* os funcionários (os mesmos valores de "Consumo por EPI" e "Entregas por obra" do 08.01) — sem sentido como ficha de uma pessoa só. Substituído pelo histórico real do próprio funcionário, mesma fonte de dado da Ficha (04.03) — mesma categoria de erro já corrigida antes (ver Sprint 7, descrição do modal de Transferência). Testado no navegador: Dashboard, Ficha e Histórico de João Carlos batendo com o Figma, estado vazio de Marcos Lima (sem histórico neste mock), filtro de Devoluções no Histórico completo, exportação CSV do Painel de relatórios e a Ficha de EPI para impressão. **Pendências desta sprint:** filtro de obra do Painel de relatórios é só de exibição (não refiltra os números ainda); "Restringir dados conforme o perfil do usuário" depende de autenticação real (Sprint 3, ainda não iniciada); "Validar relatórios com SST, RH e gestão" é um passo organizacional, fora do escopo de código.
- **07/09/2026 — Sprint 9 completa: Troca, devolução e entrega em lote (03.07, 03.08, 11.06).** Duas telas novas em `frontend/src/pages/entregas/`. `TrocaDevolucao.jsx` (03.08): o Figma não desenha um seletor de funcionário nessa tela (mostra só o texto fixo "Colaborador ativo • Obra vinculada", provavelmente porque no protótipo real ela abre a partir da ficha do funcionário — 04.03, ainda não construída) — como aqui ela só é alcançável pelo botão "Troca / devolução" da lista geral, sem contexto de funcionário, foi adicionado um passo de busca antes, reaproveitando a mesma mecânica do wizard de Nova entrega. A tabela "EPIs em posse do funcionário" com ação (Trocar/Devolver) e destino (Estoque/Descarte) por item bate com os valores exatos do Figma; ao confirmar, mostra o modal genérico `11.06 — Movimentação registrada` reaproveitando `SuccessModal.jsx` (mesmo componente já usado pela Transferência de estoque na Sprint 7). `LoteEntrega.jsx` (03.07): esta tela também só tem dado estrutural no Figma ("Funcionário 01 • matrícula" x4, sem nomes reais) — indício de que é montada dinamicamente a partir de Obra + Equipe/função, então os dois seletores aqui filtram `FUNCIONARIOS` de verdade em vez de reproduzir o texto de espaço reservado. Sugestão de EPIs vem da matriz da função, só itens "Obrigatório" (mesma regra da Sprint 8), com bloqueio de saldo total antes de continuar (RN070). Depois da configuração, um passo de "Confirmação individual" — cada funcionário confirma a própria entrega ou pode ter a confirmação marcada como falha sem travar os demais (RN071/RN072) — e uma tela final com o comprovante de cada confirmado. Testado ponta a ponta no navegador: troca com os 3 itens de João Carlos (mesma combinação Trocar/Devolver/Descarte do Figma), devolução com um funcionário sem posse cadastrada (estado vazio honesto), lote BR-040 + Servente (o único com matriz real, 1 funcionário) passando por falha simulada → nova tentativa → confirmação → conclusão, e o bloqueio de matriz vazia para uma função sem matriz definida (Motorista). Dado novo, não do Figma: `POSSE_POR_FUNCIONARIO` em `entregasConfig.js`, associando os 3 itens literais do frame 03.08 a João Carlos (matrícula 00487) — para os demais funcionários do mock, a tela mostra honestamente que não há posse detalhada, em vez de inventar itens.
- **07/09/2026 — Sprint 8 completa: Entrega individual (03.01 a 03.12 + 05.05).** Wizard único (`frontend/src/pages/entregas/NovaEntrega.jsx`) cobrindo os três passos do Figma (Funcionário → EPI e quantidade → Confirmação), a confirmação biométrica com seus 5 estados (aguardando/sem biometria/não reconhecida/leitor desconectado/confirmada, 03.05 e 03.09–03.12) como um sub-passo interno reaproveitando o mesmo componente, o bloqueio por saldo insuficiente (05.05, acionado a partir da própria entrega — RN055, resolve o risco 6 do plano de produção) e a tela de conclusão com comprovante (03.06). `EntregasLista.jsx` (03.01) traz a lista com busca e filtro por obra. Testado ponta a ponta no navegador nos dois caminhos possíveis: funcionário com biometria cadastrada (João Carlos, matriz completa da Servente, todos os 5 estados biométricos exercitados manualmente) e funcionário sem biometria (Carlos Souza, chega em "Biometria não cadastrada" e o botão "Cadastrar biometria" cai no fluxo unificado de `BiometriaCadastro.jsx` — que ganhou uma terceira origem, `"entrega"`, sem alterar o comportamento das outras duas). Dado novo, não do Figma, mas necessário para a sprint funcionar sem inventar informação na hora: `biometriaCadastrada` por funcionário e `ESTOQUE_MINIMO` por EPI (ambos documentados como tal no arquivo de origem). Corrigido no processo um gap de dados encontrado entre frames de sprints diferentes: a Matriz EPI x Função do Servente (`06.03`) lista "Colete refletivo", mas esse EPI nunca foi cadastrado na lista de EPIs (`06.01`) nem em nenhum frame de estoque (`05.x`) — sem saldo nenhum registrado, a sugestão automática da matriz bloqueava a entrega como "saldo insuficiente" para um item que na verdade nunca foi rastreado. Saldo e mínimo adicionados em `estoqueConfig.js`/`episConfig.js` com o gap documentado no comentário. Simplificações conscientes, sem tela própria no Figma para resolver: local de estoque fixo em "Almoxarifado Central" (03.03 não desenha um seletor de local), validade de CA e periodicidade não são checadas nesta sprint, e não existe caminho de "aceite já registrado" sem biometria (03.04 só desenha o botão "Confirmar com biometria"). Sessão dividida entre um agente em background (que construiu o wizard, a lista e o fio condutor dos dados) e a sessão principal (que revisou o resultado contra o Figma quadro a quadro, testou os dois caminhos no navegador, achou e corrigiu o gap do Colete refletivo) — as duas evitaram rodar dev server/navegador ao mesmo tempo na mesma aba depois de uma colisão detectada a tempo.
- **07/09/2026 — Sprint 7 completa: Transferência de estoque (05.03) + Sprint 7 encerrada.** Resolve a decisão de arquitetura pendente (risco 1 do plano de produção): construído `SuccessModal.jsx`, o primeiro componente genérico do módulo 11 — Estados (ícone de check, título, descrição, botão "Fechar"), usado como estado de sucesso da transferência em vez de duplicar `05.04`. Cálculo de saldo origem/destino ao vivo, validado contra os números exatos do Figma (Luva de raspa, 20 unidades: origem 96→76, destino 38→58). Corrigido um copiar-colar do Figma: a descrição do modal dizia "a transferência **do funcionário** foi registrada" — texto de um modal de Entregas reaproveitado por engano — substituído por um texto correto para o contexto de estoque. Saldo por local (`SALDO_POR_LOCAL`) generalizado para as três telas do módulo (Entrada, Estoque por local, Transferência).
- **07/09/2026 — Início da Sprint 7: Estoque (05.02/05.07/05.06 + substituto da 05.01 ausente).** Entrada de estoque com cálculo de saldo ao vivo (Saldo atual + Entrada = Novo saldo) — validado contra os números exatos do Figma (96 + 50 = 146). Movimentações de estoque com KPIs, busca e filtro por tipo; a tabela dessa tela tinha um bug de renderização no próprio Figma (linhas sobrepostas e texto ilegível, só a primeira linha estava legível) — as outras 4 linhas foram reconstruídas seguindo o mesmo padrão da primeira, não são dado literal do protótipo (documentado em `frontend/src/data/estoqueConfig.js`). Como não existe `05.01 — Estoque — Visão geral` no Figma (lacuna já registrada na auditoria), a rota `/estoque` foi construída com o conteúdo real do card "Estoque por local" visto no estado vazio (05.06) — pragmático em vez de deixar a rota principal do módulo sem nada. Estado vazio (05.06) virou componente compartilhado (`EstoqueVazio.jsx`) reaproveitado nas duas listagens.
- **07/09/2026 — Sprint 6 completa: EPIs, CAs e Matriz (06.01/06.02/06.03).** Lista de EPIs com busca e filtro por validade (mesmo dataset mock de 5 EPIs do Figma); Novo EPI com o painel "Pontos para validar" reproduzido tal como está no protótipo (perguntas em aberto, não funcionalidade); Matriz EPI x Função com badges coloridos (Obrigatório/Recomendado) trocando por função selecionada. Decisão consciente: só a função "Servente" tem matriz desenhada no Figma — as demais mostram "Nenhuma matriz definida para esta função ainda" em vez de dados inventados. Ficha (04.03), Histórico completo (04.04) e Transferência de obra (04.05) ficaram propositalmente pendentes da Sprint 5, adiadas para quando a Sprint 8 (Entregas) já tiver dado real de entregas para mostrar — conforme o próprio plano de produção recomendava.
- **07/09/2026 — Novo funcionário (04.02) + fluxo unificado de biometria (09.10–09.13).** Resolve a decisão de arquitetura pendente (risco 3 do plano de produção): um único componente `frontend/src/pages/cadastros/BiometriaCadastro.jsx` com passos internos (busca → captura → confirmar → sucesso) atende os dois pontos de entrada do Figma — a busca dedicada a partir do hub de Cadastros e o atalho embutido em "Novo funcionário", que já chega com o funcionário conhecido (passado via router state) e pula direto para a captura. Os textos de retorno no passo final mudam conforme a origem ("Voltar aos cadastros" vs. "Voltar ao cadastro do funcionário"). Testado ponta a ponta nos dois caminhos no navegador. `frontend/src/pages/funcionarios/NovoFuncionario.jsx` reproduz o painel lateral "Validação necessária" do Figma tal como está — é uma lista de perguntas em aberto do próprio protótipo, não uma funcionalidade.
- **07/09/2026 — Lista de Funcionários (04.01) — início da Sprint 5.** Tabela com busca (nome/matrícula/função) e filtro por obra funcionando de verdade sobre um dataset mock (`frontend/src/data/funcionariosConfig.js`, 5 funcionários — mesmos dados do Figma), já que ainda não existe backend. Clique no nome ou "+ Novo funcionário" navegam para rotas reais (`/funcionarios/:matricula`, `/funcionarios/novo`) que por enquanto mostram um aviso honesto — essas duas telas (04.02 e 04.03) dependem da decisão de arquitetura da biometria (risco 3 do plano de produção) antes de serem construídas, para não implementar cadastro biométrico duas vezes.
- **07/09/2026 — Central de Cadastros (09.01) — Sprint 4 completa.** Hub de navegação em `frontend/src/pages/cadastros/CadastrosCentral.jsx`, nova rota `/cadastros` (a rota dinâmica `/cadastros/:tipo` continua existindo para os formulários). O menu lateral "Cadastros" agora aponta para o hub em vez de ir direto pro primeiro formulário. Confirmado no Dev Mode do Figma que cada bloco navega direto para o formulário de criação daquele cadastro (não para uma lista) — os 6 já construídos apontam pros formulários reais; os que ainda não existem (Funcionários, EPIs e CAs, Usuários, Cadastrar biometria, Matriz EPI x Função) apontam pro placeholder do módulo ou caem na mensagem "cadastro não encontrado" da rota dinâmica, nunca em link morto. Uma correção consciente: os botões "Cancelar/Próximo cadastro/Salvar cadastro" que o Figma reaproveitou do template de formulário nessa tela foram removidos — não fazem sentido numa tela puramente de navegação, sem nada sendo editado. Com isso fecha a Sprint 4 (Cadastros e obras): 8 telas de cadastro + 2 de Obras + o hub, todas testadas manualmente no navegador contra o protótipo.
- **07/09/2026 — Obras: lista e detalhe (07.01/07.02).** Diferente dos cadastros mestres, essas duas telas têm layout próprio (cards + página de detalhe), não reaproveitam o `MasterForm` — novo `frontend/src/data/obrasConfig.js` + `frontend/src/pages/obras/`. Duas correções conscientes em relação ao protótipo literal: (1) no Figma as 4 telas de detalhe repetem o link "Ver funcionários →" nos 4 cards por engano de copiar-colar; aqui cada card tem o link contextual certo (Ver funcionários/Ver estoque/Ver pendências/Ver responsáveis). (2) o Figma destaca "Visão geral" no menu lateral mesmo estando na tela de Obras (mesmo tipo de descuido); aqui o destaque segue a seção real. O Figma só desenhou o detalhe operacional completo pra uma obra (BR-040); as outras 3 (Usina, Britagem, Almoxarifado Central) mostram um aviso honesto de que aquele detalhamento ainda não foi desenhado, em vez de inventar números. Botão "+ Nova obra" já navega para o cadastro de obra (09.02) que tínhamos acabado de construir — reaproveitamento real, não maquete.
- **06/09/2026 — Cadastro de obra (09.02).** Sétimo cadastro mestre, mesma mecânica: só entrada em `cadastrosConfig.js`, sem código novo. Único detalhe de conteúdo: o campo Status usa "Ativa/Inativa" (concordância com "obra"), diferente do "Ativo/Inativo" dos demais cadastros — mantido fiel ao texto do Figma em vez de padronizar à força. O campo "Responsável" de Local de estoque e Obra segue sem opções reais (dependem de Funcionários, ainda não construído).
- **06/09/2026 — Cadastro de local de estoque (09.07).** Sexto cadastro mestre, seguindo a ordem do plano de produção (depois dos 5 sem dependência, antes de Obra). Reaproveita o `MasterForm` genérico sem nenhum código novo além da entrada em `cadastrosConfig.js` — só adicionar ao objeto de configuração e à lista `CADASTROS_ORDER` foi suficiente. Campos "Obra" e "Responsável" ficam sem opções reais até `09.02 — Obra` existir.
- **06/09/2026 — Cadastros mestres simples (5 telas) + shell do app.** Construído `frontend/src/layouts/AppShell.jsx` (menu lateral + cabeçalho, cores extraídas do frame `09.05 — Cadastros — Setor` em Dev Mode: sidebar `#093826`, item ativo `#17523B`) e `frontend/src/components/MasterForm.jsx` — o componente parametrizado único recomendado pelo plano de produção, em vez de telas independentes. Cinco cadastros mestres usam esse componente com os campos exatos observados no Figma (`frontend/src/data/cadastrosConfig.js`): Setor/centro de custo (09.05), Cargo/função (09.04), Fornecedor (09.03), Categoria de EPI (09.08) e Motivo de movimentação (09.09). Roteamento adicionado (`react-router-dom`); "Entrar" no Login agora navega de verdade para `/visao-geral`, e os demais itens do menu lateral levam a uma tela "ainda não construída" honesta em vez de link morto ou 404. Selects de campos que dependem de cadastros futuros (Obra, Responsável, Setor em Cargo) ficam sem opções reais por enquanto. Bug encontrado e corrigido durante o teste manual: o formulário não resetava os campos ao navegar entre cadastros pelo botão "Próximo cadastro" (React reaproveitava a mesma instância do componente) — corrigido com `key={tipo}` em `CadastroMestre.jsx`.
- **06/09/2026 — Primeira tela implementada: Login (01.01).** Scaffold do front-end criado em `frontend/` (React + Vite + Tailwind, sem TypeScript e sem dependências além do essencial, seguindo o princípio de dependência mínima do hub). A tela reproduz o protótipo do Figma com os valores exatos extraídos via Dev Mode (cores em hex, tipografia Inter, espaçamento e raios) — ver `frontend/src/pages/Login.jsx` e os tokens em `frontend/tailwind.config.js`. O formulário é funcional (inputs controlados) mas o envio ainda não está integrado a nenhum backend — isso fica para a Sprint 3 (Autenticação e segurança), quando a arquitetura de auth for decidida. O link "Esqueci minha senha" foi mantido como visual não funcional, refletindo a lacuna já registrada em `docs/05-arquitetura/plano-de-producao-telas.md` (risco 2: não existe tela de recuperação de senha no protótipo).
- **06/09/2026 — Inspeção visual das 63 telas e plano de produção.** A auditoria anterior do Figma era estrutural (contagem de telas/reações via API); esta sessão abriu cada uma das 63 telas para ler conteúdo real (campos, textos, botões) e documentar correlação de fluxo entre elas. Resultado em `docs/05-arquitetura/plano-de-producao-telas.md`: inventário tela a tela, decisão de arquivar a tela duplicada `15 — Devolução ou troca de EPI` em favor de `03.08 — Entregas — Troca e devolução` (mais completa e já integrada à navegação), identificação de que os módulos 10 (Configurações) e 11 (Estados) e o sub-fluxo de biometria em Entregas são componentes reutilizáveis parametrizados — não telas separadas —, e ordem de construção tela a tela dentro de cada sprint já existente em `gestao/sprints/`.

## Como colaborar

O projeto também poderá ser usado por estudantes iniciantes para praticar um fluxo profissional de trabalho. Cada melhoria deverá ser registrada como uma tarefa antes do desenvolvimento.

Conhecimento prévio de programação não é obrigatório para participar das etapas de levantamento, documentação, prototipação e testes.
